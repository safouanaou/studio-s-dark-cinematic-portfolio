const toggle=document.querySelector('.menu-toggle');const nav=document.querySelector('.site-nav');const dialog=document.querySelector('.enquiry-dialog');const form=document.querySelector('.enquiry-form');const success=document.querySelector('.form-success');
function closeMenu(){toggle?.setAttribute('aria-expanded','false');nav?.classList.remove('is-open');document.body.classList.remove('nav-open');if(toggle)toggle.querySelector('span').textContent='Menu'}
toggle?.addEventListener('click',()=>{const opening=toggle.getAttribute('aria-expanded')!=='true';toggle.setAttribute('aria-expanded',String(opening));nav?.classList.toggle('is-open',opening);document.body.classList.toggle('nav-open',opening);toggle.querySelector('span').textContent=opening?'Close':'Menu'});nav?.querySelectorAll('a').forEach(link=>link.addEventListener('click',closeMenu));document.addEventListener('keydown',event=>{if(event.key==='Escape')closeMenu()});
function openEnquiry(){form.hidden=false;success.hidden=true;dialog.showModal()}function closeEnquiry(){dialog.close();form.reset()}document.querySelectorAll('[data-open-enquiry]').forEach(button=>button.addEventListener('click',openEnquiry));document.querySelectorAll('[data-close-enquiry]').forEach(button=>button.addEventListener('click',closeEnquiry));dialog?.addEventListener('click',event=>{if(event.target===dialog)closeEnquiry()});
form?.addEventListener('submit',event=>{event.preventDefault();if(!form.reportValidity())return;form.hidden=true;success.hidden=false;success.querySelector('button').focus()});const dateInput=document.querySelector('input[type=date]');if(dateInput){const today=new Date();const max=new Date(today);max.setDate(today.getDate()+60);const format=date=>date.toISOString().split('T')[0];dateInput.min=format(today);dateInput.max=format(max)}
const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}})},{threshold:.1});document.querySelectorAll('.reveal').forEach((element,index)=>{if(element.closest('.hero'))element.style.transitionDelay=`${Math.min(index*80,280)}ms`;observer.observe(element)});document.querySelector('[data-year]').textContent=new Date().getFullYear();
